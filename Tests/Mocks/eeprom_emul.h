#pragma once

/* This file wraps MockFlash.h EEPROM definitions and is placed in the Mocks directory
 * so it gets picked up instead of the real eeprom_emul.h when testing */

#include "MockFlash.h"
